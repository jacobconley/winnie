 - We will want to inject a message whenever the agent thread changes directory context
    - Maybe a directory guard hook but probably unnecessary?

# Feature set

 - Stop button
 - Exposed agent knobs
    - Sandbox mode / profile
    - Mode (agent / plan / ask)
    - Network access? 
    - Model / effort controls
